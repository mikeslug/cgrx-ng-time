export interface Entry {
  id?: number;
  project: string;
  payType: string;
  reminder: boolean;
  date: string;
  timer: boolean;
  timerTime: number;
  hours: string;
  minutes: string;
  notes: string;
}
